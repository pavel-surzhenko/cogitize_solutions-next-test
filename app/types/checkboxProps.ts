export type checkboxProps = {
    trade: boolean;
    price: boolean;
    analytics: boolean;
    duel: boolean;
    claims: boolean;
    buy: boolean;
    workers: boolean;
    position: boolean;
    kicked: boolean;
};
